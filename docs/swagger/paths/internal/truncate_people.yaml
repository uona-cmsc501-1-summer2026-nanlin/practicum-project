delete:
  tags: [Internal (testing)]
  summary: Truncate all people
  description: >
    **Internal testing only — not for production.** Deletes every row in `people`
    and resets the table auto-increment counter. Charges may reference missing
    person IDs afterward. Will be removed in a future release.
  operationId: truncatePeople
  deprecated: true
  responses:
    "200":
      description: People truncated
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/schemas/reply_success.yaml
              - type: object
                properties:
                  data:
                    $ref: ../../components/schemas/truncate_response.yaml
    "500":
      $ref: ../../components/responses/error.yaml
